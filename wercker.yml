box: wercker/nodejs
build:
  steps:
    - npm-install
    - script:
        name: Unit Tests
        code: |
          npm run test-cli

    - script:
        name: Coverage
        code: |
            echo service_name: wercker > .coveralls.yml
            echo repo_token: $COVERALLS_TOKEN >> .coveralls.yml
            make coveralls